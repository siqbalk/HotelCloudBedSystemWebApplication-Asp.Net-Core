@model IEnumerable<HotelCloudBedSystem.Areas.Manager.ViewModels.HotelImagesViewModel>


@{
    ViewData["Title"] = "HotelImages";
}


@if (Model.Count() == 0)
{
    <div class="alert alert-warning">
        No Image Found
    </div>
}
else
{

    @foreach (var image in Model)
    {

  
            @if (image.Hotelimage == null)
            {

                <img src="~/dist/img/hotel.jpg" class="card-img-top" style="height:150px;margin-left:70px" alt="woman avatar">
            }
            else
            {
                var base64 = Convert.ToBase64String(image.Hotelimage);
                var imgsrc = string.Format("data:image/gif;base64,{0}", base64);

                <img src='@imgsrc' class="card-img-top" style="height:150px;margin-left:70px;margin-top:40px" alt="User Image">
            }
    
            }
        
    }
